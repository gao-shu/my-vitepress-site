# Spring Boot 解决了什么

Boot 不是新框架，是 **约定 + 自动配置 + Starter**，把「能跑起来」的成本压下去。

## 我真正依赖它的点

- 配置外置、多环境  
- 内嵌容器，部署简单  
- Actuator、默认日志、连接池等工程默认项  

## 判断

新业务默认 Boot；纠结「纯 Spring XML」没有意义。

下一篇：[Web（MVC）与接口层](./mvc.md)  
