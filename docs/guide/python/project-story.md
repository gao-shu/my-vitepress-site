# Python 面试 · 项目讲述

---

## 填空稿

> 在 **（项目名）** 里，我用 Python 做 **（脚本/数据处理/调 API/采集验证）**。  
> 输入是 **（文件/接口/设备）**，输出是 **（报表/入库/上报）**。  
> 实现上注意了 **（超时重试 / 虚拟环境 / 日志 / 断点续跑）**。  
> 和主系统关系是 **（一次性工具 / 旁路验证 / 后来被 Java 服务替换）**。  
> 若再做，我会补 **（锁版本依赖 / 指标 / 单测）**。

---

## 可挂钩的经历方向

| 方向 | 怎么讲 | 别吹 |
|------|--------|------|
| AI 漫剧 / 多模型 | 用 Python 快速打 API、批处理提示词结果 | 「我用 Python 做了整个平台」 |
| 工业 / 采集 | 临时采集、协议验证、数据分析 | 「生产网关是 Python 集群」 |
| 上位机协作 | 和 Node/Java 分工：Python 做分析脚本 | 把 Electron 说成 Python |
| 学习项目 | 老实讲练习脚本 + 与岗位相关的点 | 虚构上线项目 |

---

## 和 Java 的分工（面试加分）

```text
Java：业务服务、事务、长期维护、权限与发布
Python：快、尖、旁路、验证、批处理
```

---

## 常见追问

- 「会 Django/Flask 吗？」→ 用过/了解路由与中间件就说程度；主栈仍是 Java Web。  
- 「数据分析？」→ pandas 会基础清洗与统计即可，别装成数据科学家。  
- 「asyncio 熟吗？」→ 按实说；不熟就讲线程池调 IO 的实践。
