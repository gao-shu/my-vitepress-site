import{_ as e,o as r,c as a,ai as n}from"./chunks/framework.ampVZV4B.js";const p=JSON.parse('{"title":"多线程与并发（JUC）核心","description":"","frontmatter":{},"headers":[],"relativePath":"java/concurrency.md","filePath":"java/concurrency.md"}'),t={name:"java/concurrency.md"};function l(s,o,i,d,c,g){return r(),a("div",null,[...o[0]||(o[0]=[n(`<h1 id="多线程与并发-juc-核心" tabindex="-1">多线程与并发（JUC）核心 <a class="header-anchor" href="#多线程与并发-juc-核心" aria-label="Permalink to “多线程与并发（JUC）核心”">​</a></h1><h2 id="本模块面试重点" tabindex="-1">本模块面试重点 <a class="header-anchor" href="#本模块面试重点" aria-label="Permalink to “本模块面试重点”">​</a></h2><ul><li><strong>线程基础</strong>：创建线程的几种方式、为什么实际开发必须用线程池，而不是到处 <code>new Thread()</code>。</li><li><strong>线程池与并发工具</strong>：<code>ThreadPoolExecutor</code> 核心参数、常见拒绝策略的含义，<code>synchronized</code> / <code>volatile</code> / <code>Lock</code> 的适用场景。</li><li><strong>JMM 与可见性</strong>：Java 内存模型、<code>happens-before</code> 大致含义、<code>volatile</code> 能做什么、不能做什么。</li><li><strong>常用并发类</strong>：<code>ConcurrentHashMap</code>、<code>CountDownLatch</code>、<code>Semaphore</code>、<code>Future</code> 等的场景。</li><li><strong>典型并发 Bug</strong>：死锁、线程安全问题、<code>volatile</code> 的误用、<code>ThreadLocal</code> 泄漏、<code>parallelStream</code> 滥用导致的性能/稳定性问题。</li></ul><hr><h3 id="❓-面试官-创建线程有哪几种方式-你们在项目里是怎么创建的" tabindex="-1">❓ 面试官：创建线程有哪几种方式？你们在项目里是怎么创建的？ <a class="header-anchor" href="#❓-面试官-创建线程有哪几种方式-你们在项目里是怎么创建的" aria-label="Permalink to “❓ 面试官：创建线程有哪几种方式？你们在项目里是怎么创建的？”">​</a></h3><p><em>频率：🔥🔥🔥🔥</em></p><p><strong>💡 一句话总结（先抛结论）：</strong> 基础方式有三种：继承 <code>Thread</code> 类、实现 <code>Runnable</code> 接口、实现 <code>Callable</code> 接口（配合 <code>FutureTask</code> 可获取返回值）。但<strong>在实际企业项目中，我们绝对不会显式地去 <code>new Thread()</code>，而是 100% 必须通过「线程池」来创建和管理线程。</strong></p><p><strong>📝 详细原理解析：</strong></p><ol><li><strong>继承 <code>Thread</code> vs 实现 <code>Runnable</code></strong>： <ul><li>继承 <code>Thread</code> 扩展性差，因为 Java 只支持单继承。</li><li>实现 <code>Runnable</code> 更好，还可以去继承其他的业务类，且方便实现多线程之间的数据共享。</li></ul></li><li><strong><code>Runnable</code> vs <code>Callable</code></strong>： <ul><li><code>Runnable</code> 的 <code>run()</code> 方法没有返回值，也无法向外抛出受检异常。</li><li><code>Callable</code> 的 <code>call()</code> 方法有泛型返回值，且支持 <code>throws Exception</code>。通常用于需要拿到多线程并发计算结果的场景（如并行调用三个接口，最后汇总结果）。</li></ul></li></ol><p><strong>🌟 面试加分项（阿里规约）：</strong> &quot;《阿里巴巴 Java 开发手册》强制规定：线程资源必须通过线程池提供，不允许在应用中自行显式创建线程。因为每次 <code>new Thread()</code> 都会消耗几兆的内存，且销毁线程也要耗费 CPU。如果不加控制，遇到突发流量瞬间创建几万个线程，会直接把服务器内存撑爆（OOM），或者导致 CPU 上下文切换过度而死机。&quot;</p><hr><h3 id="❓-面试官-那你能详细讲讲线程池的核心参数吗-threadpoolexecutor-【中高级】" tabindex="-1">❓ 面试官：那你能详细讲讲线程池的核心参数吗？（ThreadPoolExecutor）【中高级】 <a class="header-anchor" href="#❓-面试官-那你能详细讲讲线程池的核心参数吗-threadpoolexecutor-【中高级】" aria-label="Permalink to “❓ 面试官：那你能详细讲讲线程池的核心参数吗？（ThreadPoolExecutor）【中高级】”">​</a></h3><p><em>频率：🔥🔥🔥🔥🔥</em></p><p><strong>💡 一句话总结（先抛结论）：</strong><code>ThreadPoolExecutor</code> 有 7 个核心参数。最重要的三个是：<strong>核心线程数（corePoolSize）</strong>、<strong>最大线程数（maximumPoolSize）</strong>、<strong>阻塞队列（workQueue）</strong>。它们共同决定了线程池面对高并发时的处理策略。</p><p><strong>📝 详细参数与执行流程（必背）：</strong> 当一个新的任务提交到线程池时，它的处理流转如下：</p><ol><li><strong><code>corePoolSize</code>（核心线程数）</strong>：如果当前干活的线程数 &lt; 核心数，<strong>立刻创建新线程</strong>去执行这个任务（哪怕有其他空闲线程也不管）。</li><li><strong><code>workQueue</code>（阻塞队列）</strong>：如果核心线程全都在忙，新来的任务会被<strong>扔进阻塞队列</strong>里排队等待。</li><li><strong><code>maximumPoolSize</code>（最大线程数）</strong>：如果阻塞队列也<strong>塞满了</strong>，且当前线程数 &lt; 最大线程数，线程池会<strong>“破例”创建非核心（救急）线程</strong>来立刻处理这个任务。</li><li><strong><code>handler</code>（拒绝策略）</strong>：如果队列满了，且救急线程也达到最大数量了，说明彻底崩盘了，就会触发拒绝策略。 <ul><li><code>AbortPolicy</code>（默认）：直接抛出异常，阻止系统正常运行。</li><li><code>CallerRunsPolicy</code>：谁提交的任务（比如主线程），谁自己去执行它（起到限流作用）。</li><li><code>DiscardPolicy</code>：直接把任务丢弃，当没发生过。</li><li><code>DiscardOldestPolicy</code>：踢掉队列里排队最久（最老）的那个任务，把新任务塞进去。</li></ul></li><li><strong><code>keepAliveTime</code> &amp; <code>unit</code></strong>：救急线程空闲了多长时间后，会被自动回收销毁。</li><li><strong><code>threadFactory</code></strong>：线程工厂，用来给线程起个有意义的业务名字（排查死锁日志时非常有用）。</li></ol><hr><h3 id="❓-面试官-java-里有哪些锁-能讲讲-synchronized-和-reentrantlock-的区别吗-【中级】" tabindex="-1">❓ 面试官：Java 里有哪些锁？能讲讲 synchronized 和 ReentrantLock 的区别吗？【中级】 <a class="header-anchor" href="#❓-面试官-java-里有哪些锁-能讲讲-synchronized-和-reentrantlock-的区别吗-【中级】" aria-label="Permalink to “❓ 面试官：Java 里有哪些锁？能讲讲 synchronized 和 ReentrantLock 的区别吗？【中级】”">​</a></h3><p><em>频率：🔥🔥🔥🔥🔥</em></p><p><strong>💡 一句话总结（先抛结论）：</strong><code>synchronized</code> 是 Java 语言内置的<strong>关键字（隐式锁）</strong>，基于 JVM 层面实现，使用最简单；<code>ReentrantLock</code> 是 JUC 包下的<strong>API 类（显式锁）</strong>，基于 AQS 底层实现，功能更强大灵活（支持公平锁、响应中断、超时尝试）。</p><p><strong>📝 详细对比：</strong></p><table tabindex="0"><thead><tr><th>维度</th><th><code>synchronized</code> (内置锁)</th><th><code>ReentrantLock</code> (API 锁)</th></tr></thead><tbody><tr><td><strong>底层实现</strong></td><td>JVM 层面的 Monitor 对象（C++ 实现）</td><td>Java 层面的 AQS 机制（CAS + 队列）</td></tr><tr><td><strong>加锁释放</strong></td><td>自动加锁，代码块执行完/抛异常自动释放，<strong>绝不会死锁</strong></td><td>必须手动 <code>lock()</code>，且必须在 <code>finally</code> 中 <code>unlock()</code>，否则可能死锁</td></tr><tr><td><strong>灵活性</strong></td><td>只能是非公平锁、不可中断</td><td>可配置公平/非公平、支持 <code>lockInterruptibly()</code> 响应中断、支持 <code>tryLock(time)</code> 超时放弃</td></tr><tr><td><strong>锁升级优化</strong></td><td>JDK 6 引入了偏向锁 $\\rightarrow$ 轻量级锁 $\\rightarrow$ 重量级锁的自动升级过程</td><td>始终是重量级控制，但由于是 Java 代码，自旋逻辑更可控</td></tr></tbody></table><hr><h3 id="❓-面试官-什么是-volatile-关键字-它能保证线程安全吗-【中高级】" tabindex="-1">❓ 面试官：什么是 volatile 关键字？它能保证线程安全吗？【中高级】 <a class="header-anchor" href="#❓-面试官-什么是-volatile-关键字-它能保证线程安全吗-【中高级】" aria-label="Permalink to “❓ 面试官：什么是 volatile 关键字？它能保证线程安全吗？【中高级】”">​</a></h3><p><em>频率：🔥🔥🔥🔥</em></p><p><strong>💡 一句话总结（先抛结论）：</strong><code>volatile</code> 是 Java 最轻量级的同步机制。它只能保证变量的<strong>可见性</strong>和<strong>有序性（禁止指令重排）</strong>，但<strong>不能保证原子性</strong>！因此，它<strong>不能</strong>完全替代锁来保证线程安全（比如 <code>volatile int i; i++</code> 依然是不安全的）。</p><p><strong>📝 详细原理解析：</strong></p><ol><li><strong>可见性（保证读到最新值）</strong>： <ul><li>现代 CPU 都有多级缓存（L1/L2 Cache）。Java 线程在读取主内存变量时，会先拷贝一份到自己的工作内存里。</li><li>线程 A 修改了变量，没有及时刷回主内存；线程 B 读的依然是自己工作内存里的旧值（脏读）。</li><li>加了 <code>volatile</code> 后：<strong>强制线程每次写完立刻刷回主内存，强制其他线程每次读都必须直接去主内存拉取最新值</strong>。</li></ul></li><li><strong>有序性（禁止指令重排）</strong>： <ul><li>编译器和 CPU 为了性能，有时候会把我们写的代码顺序打乱（比如把毫不相干的两行代码颠倒执行）。</li><li>在单线程下没问题，但在多线程（最经典的 DCL 单例模式）下会导致灾难。<code>volatile</code> 底层通过插入<strong>内存屏障（Memory Barrier）</strong>来禁止前后指令重排序。</li></ul></li><li><strong>为什么不能保证原子性？</strong><ul><li><code>i++</code> 其实分为三步：取值、加 1、写回。</li><li><code>volatile</code> 只保证取值那一瞬间是最新的。但在加 1 的过程中，如果另一个线程抢先写回了新值，当前线程再写回时依然会发生数据覆盖。保证原子性必须用 <code>synchronized</code> 或者 <code>AtomicInteger</code>（CAS）。</li></ul></li></ol><hr><h3 id="❓-面试官-你刚刚提到了-cas-能说说什么是-cas-吗-它有什么缺点-aba-问题-【高级】" tabindex="-1">❓ 面试官：你刚刚提到了 CAS，能说说什么是 CAS 吗？它有什么缺点？（ABA 问题）【高级】 <a class="header-anchor" href="#❓-面试官-你刚刚提到了-cas-能说说什么是-cas-吗-它有什么缺点-aba-问题-【高级】" aria-label="Permalink to “❓ 面试官：你刚刚提到了 CAS，能说说什么是 CAS 吗？它有什么缺点？（ABA 问题）【高级】”">​</a></h3><p><em>频率：🔥🔥🔥</em></p><p><strong>💡 一句话总结（先抛结论）：</strong> CAS（Compare And Swap，比较并交换）是一种<strong>无锁（乐观锁）</strong>的原子并发机制。它包含三个操作数：内存地址 V、旧的预期值 A、准备写入的新值 B。<strong>当且仅当 V 处的值等于 A 时，才把 V 的值更新为 B</strong>。</p><p><strong>📝 详细原理解析与缺点：</strong></p><ul><li><strong>底层原理</strong>：CAS 不是 Java 实现的，它是直接调用底层的 <code>Unsafe</code> 类，映射到了底层 CPU 提供的硬件级别的原子指令（如 x86 的 <code>cmpxchg</code> 指令），绝对保证操作原子性且不需要挂起线程。</li><li><strong>三大缺点（必考坑点）</strong>： <ol><li><strong>自旋开销大</strong>：如果运气不好，一直有别的线程在修改值，CAS 会一直失败并进入 <code>while(true)</code> 死循环（自旋），极大地消耗 CPU 资源。</li><li><strong>只能保证一个共享变量</strong>：如果要同时原子的修改多个变量，CAS 无能为力，必须上 <code>synchronized</code> 锁。</li><li><strong>ABA 问题（致命大坑）</strong>： <ul><li>线程 1 读到共享变量的值是 A，准备改成 C。</li><li>就在这时，线程 2 跑过来，先把 A 改成了 B，紧接着又把 B 改回了 A！</li><li>线程 1 醒来执行 CAS，发现变量的值还是 A（其实已经是被修改过无数次又变回来的伪装 A），于是很开心地修改成了 C。</li><li><strong>解决方案</strong>：引入版本号（Version）。每次修改变量时版本号 +1（A1 -&gt; B2 -&gt; A3）。JDK 提供了 <code>AtomicStampedReference</code> 类来解决这个问题，CAS 比较时不仅比对值，还必须比对版本号。</li></ul></li></ol></li></ul><hr><h3 id="❓-面试官-什么是-java-内存模型-jmm-happens-before-大概是什么意思-【中级】" tabindex="-1">❓ 面试官：什么是 Java 内存模型（JMM）？“happens-before” 大概是什么意思？【中级】 <a class="header-anchor" href="#❓-面试官-什么是-java-内存模型-jmm-happens-before-大概是什么意思-【中级】" aria-label="Permalink to “❓ 面试官：什么是 Java 内存模型（JMM）？“happens-before” 大概是什么意思？【中级】”">​</a></h3><p><em>频率：🔥🔥🔥</em></p><p><strong>💡 一句话总结（先抛结论）：</strong> JMM 规定了<strong>线程之间如何通过内存（主内存 + 工作内存）来进行通信</strong>，以及哪些操作在别的线程看来一定是“先发生”的，这套规则就叫 <code>happens-before</code>。只要能说清几条常见规则（<code>synchronized</code>、<code>volatile</code>、线程启动/终止），就够应付大部分初/中级面试。</p><p><strong>📝 可以重点记住的几条 happens-before 规则：</strong></p><ol><li><strong>程序次序规则</strong>：在同一个线程内，代码按顺序执行，前面的操作对后面的操作可见。</li><li><strong>锁定规则</strong>：对同一把锁的 <strong>解锁</strong> <code>unlock</code> 一定先于后面对这把锁的 <strong>加锁</strong> <code>lock</code>（后面的线程能看到前面线程在锁内做的修改）。</li><li><strong>volatile 变量规则</strong>：对某个 <code>volatile</code> 变量的写，一定先于后面对这个变量的读（保证可见性和有序性）。</li><li><strong>线程启动规则</strong>：<code>Thread.start()</code> 之前对共享变量的写，对新线程是可见的。</li><li><strong>线程终止规则</strong>：线程在结束前对共享变量的写，对调用 <code>thread.join()</code> 的线程是可见的。</li></ol><blockquote><p>面试时不需要把所有规则背下来，更重要的是能举 1～2 个例子，说明你知道“为什么加了 synchronized / volatile 后，别的线程就能看到最新值”。</p></blockquote><hr><h3 id="❓-面试官-说说你们项目里是怎么用-threadlocal-的-有什么需要注意的-【中级】" tabindex="-1">❓ 面试官：说说你们项目里是怎么用 ThreadLocal 的？有什么需要注意的？【中级】 <a class="header-anchor" href="#❓-面试官-说说你们项目里是怎么用-threadlocal-的-有什么需要注意的-【中级】" aria-label="Permalink to “❓ 面试官：说说你们项目里是怎么用 ThreadLocal 的？有什么需要注意的？【中级】”">​</a></h3><p><em>频率：🔥🔥🔥</em></p><p><strong>💡 一句话总结（先抛结论）：</strong><code>ThreadLocal</code> 用来在<strong>同一线程内传递上下文数据</strong>（比如登录用户信息、traceId 等），避免到处传参。最大的问题是：在线程池里如果<strong>不及时清理</strong>，很容易导致内存泄漏和“脏数据”问题。</p><p><strong>📝 常见使用场景：</strong></p><ul><li>保存当前登录用户信息：在过滤器/拦截器里解析出用户信息，放入 <code>ThreadLocal</code>，后面的 Service 直接取。</li><li>保存一次请求的 traceId：方便链路日志串起来。</li></ul><p><strong>⚠️ 必须注意的几个坑：</strong></p><ol><li><strong>线程池复用线程</strong>：请求结束了，线程却不会销毁；如果不 <code>remove()</code>，下一个请求复用这个线程时，可能还能读到上一个请求留下来的旧数据。</li><li><strong>一定要在 finally 里清理</strong>：<div class="language-java line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    UserContext.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(user);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 业务逻辑</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">finally</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    UserContext.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">remove</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div></li><li><strong>不要存放大对象/集合</strong>：否则容易撑爆内存，而且不容易排查。</li></ol><blockquote><p>面试时可以准备一个“小故事”：比如线上排查过因为 ThreadLocal 没清导致用户串号/内存飙升的案例，会非常加分。</p></blockquote><hr><h3 id="❓-面试官-你实际遇到过哪些并发-bug-是怎么排查和解决的-【开放题】" tabindex="-1">❓ 面试官：你实际遇到过哪些并发 Bug？是怎么排查和解决的？【开放题】 <a class="header-anchor" href="#❓-面试官-你实际遇到过哪些并发-bug-是怎么排查和解决的-【开放题】" aria-label="Permalink to “❓ 面试官：你实际遇到过哪些并发 Bug？是怎么排查和解决的？【开放题】”">​</a></h3><p><em>频率：🔥🔥🔥🔥</em></p><p>这一题没有标准答案，但几乎所有中级面试都会问，可以提前准备 1～2 个简单案例，例如：</p><ul><li><p><strong>案例 1：多线程写 Map 导致偶发现象</strong></p><ul><li>问题：一开始用了 <code>HashMap</code> 做缓存，在多线程写入时偶尔出现数据丢失/死循环。</li><li>排查：发现业务是多线程并发写，改成 <code>ConcurrentHashMap</code>，同时优化了加锁粒度。</li><li>总结：线程不安全的集合在并发写入场景一定要慎用。</li></ul></li><li><p><strong>案例 2：线程池参数设置不合理导致任务堆积</strong></p><ul><li>问题：核心线程太少、队列太大，导致高峰期请求都在队列里排队，整体响应时间飙升。</li><li>解决：根据 QPS 重新估算 <code>corePoolSize</code> / 队列长度，引入超时和降级策略（<code>CallerRunsPolicy</code> 或直接拒绝 + 友好提示）。</li><li>总结：线程池不是随便 new，一个重要经验是<strong>宁可早点失败，也不要悄悄排队排死整个系统</strong>。</li></ul></li></ul>`,55)])])}const u=e(t,[["render",l]]);export{p as __pageData,u as default};
